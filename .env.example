# Steam Web API key, used server-side by the /api functions only. Never exposed
# to the browser. Get one at https://steamcommunity.com/dev/apikey
# Set this in the Vercel project env (Settings -> Environment Variables), and in
# a local .env.local for `vercel dev`.
STEAM_API_KEY=
